name: CI/CD GPIT

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    env:
      NODE_ENV: production

    steps:
      # 1️⃣ Cloner le dépôt
      - name: 📦 Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Installer Node.js
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3️⃣ Installer sshpass pour le déploiement
      - name: 🔑 Installer sshpass
        run: sudo apt-get update && sudo apt-get install -y sshpass

      # 4️⃣ Installer backend
      - name: ⚙️ Install backend
        working-directory: ./backend
        run: npm ci

      # 5️⃣ Lancer tests backend
      - name: 🧪 Test backend
        working-directory: ./backend
        run: npm test || echo "⚠️ Aucun test défini, on continue"

      # 6️⃣ Installer frontend
      - name: ⚙️ Install frontend
        working-directory: ./frontend
        run: npm ci

      # 7️⃣ Build frontend
      - name: 🧱 Build frontend
        working-directory: ./frontend
        run: npm run build

      # 8️⃣ Analyse SonarCloud
      - name: 🔍 SonarCloud Analysis
        uses: sonarsource/sonarcloud-github-action@v2
        with:
          projectKey: gpit
          organization: EmnaMansour
          token: ${{ secrets.SONAR_TOKEN }}

      # 9️⃣ Déploiement SSH
      - name: 🚀 Déploiement sur serveur
        if: success()
        env:
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_PASS: ${{ secrets.SERVER_PASS }}
        run: |
          sshpass -p "$SERVER_PASS" ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "
            cd /var/www/gpit &&
            git reset --hard &&
            git pull origin main &&
            npm ci --prefix backend &&
            npm run build --prefix frontend &&
            pm2 restart all
          "

      # 🔟 Test post-déploiement backend
      - name: 🔍 Test post-déploiement backend
        if: success()
        env:
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
        run: |
          echo "Test du backend..."
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://$SERVER_IP:$SERVER_PORT/api/health)
          if [ "$RESPONSE" -ne 200 ]; then
            echo "❌ Backend non opérationnel !"
            exit 1
          else
            echo "✅ Backend OK"

      # 1️⃣1️⃣ Test post-déploiement frontend
      - name: 🔍 Test post-déploiement frontend
        if: success()
        env:
          SERVER_IP: ${{ secrets.SERVER_IP }}
        run: |
          echo "Test du frontend..."
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://$SERVER_IP)
          if [ "$RESPONSE" -ne 200 ]; then
            echo "❌ Frontend non opérationnel !"
            exit 1
          else
            echo "✅ Frontend OK"
